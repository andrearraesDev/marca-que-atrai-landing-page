import React from 'react';

// Componente de Navbar vazio
// Este componente foi criado para remover a barra branca, sem renderizar conteúdo na tela.
// Caso a barra branca esteja associada a algum outro comportamento, é importante revisar
// a estrutura do layout ou a forma como o componente é utilizado na aplicação.

const Navbar = () => {
  return null; // Retorna 'null' para não renderizar nada no DOM
};

export default Navbar; // Exporta o componente para ser usado em outras partes do projeto
